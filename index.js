// This code imports the CLI module from the file './lib/cli.js'
// Then creates an instance of the CLI class and assigns it to a variable named cli and runs the method. 

const CLI = require('./lib/cli.js');

const cli = new CLI();

cli.run();
 